import { EuroFormatierungPipe } from './euro-formatierung.pipe';

describe('EuroFormatierungPipe', () => {
  it('create an instance', () => {
    const pipe = new EuroFormatierungPipe();
    expect(pipe).toBeTruthy();
  });
});
